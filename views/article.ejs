<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= article.title %></title>
  <style>
    body { font-family: sans-serif; font-size: 14px; margin: 10px; line-height: 1.5; }
    h1 { font-size: 16px; font-weight: bold; margin: 5px 0 10px 0; }
    .back { font-size: 12px; margin-bottom: 10px; }
    .back a { color: #666; text-decoration: none; }
    .content { margin-top: 10px; }
    .content img { max-width: 100%; height: auto; margin: 10px 0; }
    .content a { color: #000; text-decoration: underline; }
    .content p { margin: 8px 0; }
    .content h2, .content h3 { font-size: 15px; margin: 10px 0 5px 0; }
  </style>
</head>
<body>
  <div class="back">
    <a href="/">← 返回</a>
  </div>
  
  <h1><%= article.title %></h1>
  
  <div class="content">
    <%- article.content %>
  </div>
</body>
</html>
